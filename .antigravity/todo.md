# 📋 FigmaAI: Tactical Memory (TODO)

## Активные задачи
- [ ] Добавить модальное окно подробного просмотра и ручного редактирования досье ученика (кнопка `✏️` рядом с селектором ученика).
- [ ] В перспективе (будущий этап): интеграция с Google Calendar для синхронизации расписания и статуса готовности уроков.

## Недавно выполнено
- [x] Реализована система резервного копирования и восстановления базы учеников (Student CRM Backup & Restore) через Telegram и API:
  - Сервис `server/services/backup_service.py`: сборка самодостаточного `.zip` архива всех досье учеников (`data/students/`) с манифестом `BACKUP_MANIFEST.json` и защитой от Zip-Slip.
  - Подробная наглядная подпись в Telegram: дата создания, общий размер, список учеников с именами, возрастом, уровнем, количеством пройденных уроков и интересами, а также инструкция по восстановлению.
  - Команда `/backup` и распознавание естественных фраз («бэкап», «сохрани базу», «выгрузи учеников») в Telegram-боте.
  - Автоматическое восстановление: при пересылке `.zip` архива боту он проверяет файлы, восстанавливает базу учеников и выводит отчет.
  - Эндпоинты в `server/main.py`: `GET /api/backup/download`, `POST /api/backup/create`, `POST /api/backup/restore`.
- [x] Внедрен компактный счетчик расхода токенов под ответами в Telegram-боте (`server/telegram_bot.py`, `server/agents/chat_agent.py`):
  - Точный подсчет входящих и исходящих токенов (`o200k_base` / `ai_engine.count_tokens`) за каждый запрос.
  - Накопительный счетчик расхода за текущую сессию пользователя (`📊 Токены: ▫️ Запрос: ~320 ▫️ Сессия: ~1.8k`).
- [x] Диалоговый режим Telegram-бота переведен на свободный, естественный стиль общения (`server/agents/chat_agent.py`):
  - Бот общается как живой, умный напарник-человек, готов беседовать на любые темы без навязывания уроков и без спама анкетами «Какой уровень?».
- [x] Создан автоматический установщик `install.bat` (версия `0.11.0-beta.2`): интерактивный запрос папки установки (по умолчанию `C:\FigmaAI`), автозагрузка Python 3.11 при отсутствии, проверка Antigravity IDE (Google OAuth без API-ключей), авторазвертывание `.venv`, установка библиотек, создание ярлыка на Рабочем столе и отказ от смены IP в пользу `localhost`.
- [x] Организована релизная стратегия с веткой `beta` (Rule 74): создана ветка `beta`, файл `CHANGELOG.md`, текущая версия повышена до `0.11.0-beta.2`. Все новые функции и коммиты направляются в `beta`, ветка `main` остается стабильной до прямой команды пользователя на релиз.
- [x] Комплексная оптимизация потребления токенов и задержки (Token Efficiency & Latency Optimization):
  - **Vision-пайплайн (`vision_agent.py`)**: внедрена функция `optimize_image_for_vision` (масштабирование до 800-1024px JPEG, 85 quality перед отправкой в Gemini Multimodal API). Сокращает расход токенов на 60%+ (с 6+ до 1-2 тайлов на изображение), сохраняя 100% точность OCR и оригинальные HD-картинки для холста Figma.
  - **Комплексный урок по Блуму (`content_agent.py`, `orchestrator.py`)**: реализована единая генерация `generate_unified_bloom_lesson` (1 вызов LLM вместо 6 последовательных), снизившая расход токенов с ~10,000 до ~3,300 (-67%) и задержку в 3 раза с идеальным сквозным переиспользованием лексики. Сохранен 100% безопасный фолбэк на пошаговую генерацию.
  - **Fast-Path роутинг команд (`orchestrator.py`)**: эвристический анализ для коротких команд (<= 12 слов) с известным типом блока и уровнем минует LLM-диспетчер (-450 токенов, -3 сек).
  - **Сжатие системного промпта чата (`chat_agent.py`)**: системный промпт сокращен с ~950 до ~350 токенов на каждое сообщение (-60%) с полным сохранением поведения, JSON-схемы, уточнения CEFR и suggested_replies.
- [x] Реализована синхронизация с GitHub ([YarNSD/FigmaAI_ESL-v2](https://github.com/YarNSD/FigmaAI_ESL-v2)) и система обновлений по воздуху (OTA Updater) через Web UI:
  - **Защита конфиденциальности и безопасность**: в `.gitignore` надежно изолированы `config.json` (с токенами Telegram), база учеников `data/students/`, виртуальные окружения и кэши; создан чистый шаблон `config.example.json` и `VERSION` (`0.10.0`).
  - **Автоматизация релизов (`scripts/release.py`)**: публикация в 1 команду (`python scripts/release.py 0.11 "описание"`). Автоматически запускает GSD-верификатор, бампает `VERSION`, делает git commit/push и создает официальный GitHub Release через `gh release create`.
  - **Бэкенд сервис (`server/services/updater.py`, `server/main.py`)**: `GET /api/system/check-update` опрашивает открытый GitHub API без необходимости авторизации, сравнивает семантические версии, а `POST /api/system/apply-update` безопасно выполняет `git pull origin main`, обновление pip зависимостей, синхронизацию плагина и верификацию.
  - **Интерфейс панели управления (`web_ui`)**: бейдж версии в сайдбаре (`🏷️ v0.10.0`), всплывающий баннер с подсветкой при выходе релиза, модальное окно с отображением Markdown списка изменений (Changelog), кнопка `[🚀 Обновить сейчас]` с живым консольным логом и перезагрузкой.

  - **Выделенные картинки с холста**: преподаватель выделяет фото на доске FigJam/Figma и пишет в Telegram («сделай упражнение по выделенным картинкам»). Сервер через `bridge.get_selected_images()` выгружает растры в JPG и передает их в Vision-пайплайн.
  - **Приём фото и альбомов в Telegram**: бот принимает одиночные фото и альбомы (`media_group_id` с дебаунсингом 700ms), сохраняет в буфер сессии и выводит кнопки выбора формата (`🎯 Квиз`, `🗣️ Speaking Cards`, `📚 Таблица словаря`, `🃏 Флешкарты`).
  - **Пакетный Vision-агент (`analyze_batch_images`)**: анализирует до 15 картинок разом через Gemini Multimodal Vision, формулирует релевантные вопросы/карточки 1-в-1 к каждому фото и вшивает `image_base64` прямо в элементы Figma.
- [x] Полное устранение боковой полосы прокрутки (Zero Scrollbar) и точный динамический авторазмер окна:
  - **Устранение полосы прокрутки**: на `html`, `body` и все элементы выставлены `overflow: hidden !important;`, `scrollbar-width: none !important;` и сброс скроллбаров `-webkit-scrollbar { display: none !important; }`. Плашка тостов `.toast-pill` вынесена в `position: fixed` и не добавляет скрытой высоты.
  - **Истинный динамический размер**: измерение высоты переведено на `activeView.getBoundingClientRect().height` активной панели (`mainView` / `settingsView`) с моментальной адаптацией в `figma.ui.resize()` без пустых зон и срезания кнопок.
- [x] Реализован переключатель области бэкапа: «Вся доска» и «Только выделенные элементы»:
  - В окно бэкапа добавлен двухкнопочный сегментированный переключатель (`🌐 Вся доска` / `🎯 Выделенное`) с живым бейджем числа выделенных элементов (`figma.on("selectionchange")`).
  - При выборе «Только выделенные элементы» в бэкап попадают строго выделенные на холсте карточки/блоки. При попытке бэкапа без выделения выводится подсказка-предупреждение.
  - При восстановлении бэкапа выделенных элементов холст не очищается — элементы аккуратно добавляются на текущую доску поверх существующих материалов.
- [x] Реализован динамический размер окна плагина (Auto-Resize), упрощенный выбор «Упаковать в ZIP» по умолчанию и всплывающая инструкция для новичков (`ui.html`, `code.js`):
  - **Динамический размер окна**: окно автоматически меняет высоту (`RESIZE_UI`) в зависимости от открытого экрана (158px для пульта управления, 530+ px для экрана настроек и бэкапа) с помощью `ResizeObserver` и `requestAnimationFrame`. Исключено обрезание кнопок и текста на любых масштабах экрана Windows.
  - **Упаковать в ZIP по умолчанию**: убран вложенный чекбокс, опция переименована в «Упаковать в ZIP» и выбрана по умолчанию. При выборе создается быстрый архив без нагрузки на процессор.
  - **Инструкция для новичков**: рядом с заголовком раздела бэкапа добавлена кнопка `[?]`, при наведении или клике на которую открывается интерактивное пошаговое руководство по созданию и правильному восстановлению копии доски.
- [x] Реализована поддержка двух форматов резервного копирования доски и встроенный автономный Micro ZIP-движок (`ui.html`, `code.js`):
  - **Формат 1**: Единый файл `.json` (все элементы и медиа упакованы внутри в Base64) — для быстрой отправки.
  - **Формат 2**: Раздельный пакет — чистый легковесный `board.json` (1-2 МБ) + оригиналы фото отдельными файлами в папке `media/`.
  - **Опция «Упаковать в ZIP (без сжатия)»**:
    - При включенной галочке (Вариант А): упаковка в `.zip` архив (внутри `board.json` и папка `media/`, режим STORE без нагрузки на CPU).
    - При снятой галочке (Вариант Б): сохранение напрямую в выбранную папку на диске Windows (`showDirectoryPicker`).
  - **Автономный Micro ZIP-движок**: написан на чистом JS, zero-dependency, 100% офлайн, собирает и читает ZIP за доли секунды.
  - **Универсальное восстановление**: кнопки выбора «Файл (.json/.zip)» и «Папка на диске» с обязательным модальным окном подтверждения (имя, дата, число элементов и медиа).
- [x] Реализована функция сохранения бэкапа в файл на ПК и восстановления из файла с подтверждением (`ui.html`, `code.js`, `bridge.py`):
  - Интерфейс максимально лаконичен: убраны ручные поля ввода путей из окна плагина.
  - При сохранении бэкапа открывается системное окно Windows («Сохранить как...» / `showSaveFilePicker`), где пользователь сам выбирает любую папку на компьютере и имя файла.
  - Полная сериализация узлов доски (`serializeNode`): геометрия, стили, скругления, шрифты, роли и картинки.
  - При восстановлении: открывается проводник Windows, пользователь выбирает файл.
  - Обязательное защитное модальное окно подтверждения («⚠️ Восстановить доску из файла?») с предупреждением о стирании текущих материалов.
  - Десериализация (`deserializeNode`): чистое пошаговое восстановление доски с автоподгрузкой шрифтов и центрированием камеры.
- [x] Реализована система педагогического дебрифинга (Итоги урока) и сбора данных об учениках (`feedback_agent.py`, `student_agent.py`, `telegram_bot.py`, `main.py`, `web_ui`):
  - Живой диалог-интервью в Telegram-боте (кнопка `[📝 Итоги урока]`, команды `/feedback`, `/review`, `/итоги`) и в панели Web UI (вкладка «📝 Итоги урока»).
  - Голосовой ввод по **Варианту А** (Google Gemini 2.0 Flash Multimodal Audio API, ~32 токена/сек в рамках подписки Pro, без нагрузки на локальный процессор): голосовые заметки Telegram `.ogg` и запись через микрофон в браузере мгновенно расшифровываются и анализируются.
  - Пошаговое ведение преподавателя: бот задает конкретные вопросы шаг за шагом (ученик + номер блока 1.1, 5.2 -> механика и реакция -> конкретные языковые ошибки -> концентрация и план на след. урок) с удобными кнопками-подсказками.
  - Автоматическое сохранение сильных сторон, зон роста и истории урока в досье ученика (`profile.json`, `lessons.json`).
  - В Web UI добавлена полноценная панель дебрифинга: чат дебрифинга, запись голоса прямо из браузера, карточка досье ученика (сильные стороны, зоны роста, история прошлых занятий) в реальном времени.
- [x] Полностью переработан Telegram-чат и диалоговый ассистент (`chat_agent.py`, `pedagogical_knowledge.py`, `telegram_bot.py`): ИИ переведен в живой, теплый, непринужденный стиль дружелюбного коллеги-педагога. Полностью устранены непрошенные 45-минутные планы уроков и навязанные планы создания. ИИ теперь задает точечные уточняющие вопросы под ситуацию (формат, настроение, фокус fluency/accuracy, особенности ученика, стиль и объем), предлагает удобные контекстные кнопки-ответы (`suggested_replies`), а к сборке на доске (`ready_to_build: True`) переходит строго после явного подтверждения преподавателя.
- [x] Создана полная резервная копия проекта и всех файлов: `C:\Users\Admin\Figma резерв 2` (1391 каталог, 12650 файлов).
- [x] Исправлен z-index перетаскиваемых кнопок со словами (`draggable_word_chip`) в блоке Fill in the Blanks (`91:1701`): все чипы перемещены на самый верхний слой группы (`grp.appendChild(chip)`), теперь при перетаскивании мышкой на предложения кнопки всегда парят поверх всех карточек предложений; полностью устранены заполнители `extra_5`–`extra_10`, банк слов и предложения наполнены 10 уникальными контекстными словами и фразами.
- [x] Блок квиза на доске переработан и доведен до идеала (`91:1570`): исправлен z-index (подложка карточки создается первой, текст вопросов на 100% виден на переднем плане), внедрена компактная 2-колоночная сетка 5×2 (высота сокращена с 5014px до ~1780px), исключено зацикливание и дубликаты (10 уникальных вопросов с реальными живыми сценами), удален шаблон «None of the above», убраны буквы A/B/C/D с кнопок, добавлена обязательная конвертация картинок в JPEG для защиты от ошибки `Image type is unsupported` в Figma при получении WEBP.
- [x] Реализована привязка координат карточек (`speaking_card`) и плашек (`draggable_word_chip`) к текущему положению перемещенного блока при сбросе: карточки возвращаются в аккуратную стопку в центр блока на новых координатах без улетания на старое место и без раздувания рамки выделения.
- [x] Активирован компактный лаконичный пульт управления Figma-плагина (размер 310×160 px): строка статуса (точка ONLINE/OFFLINE, имя доски), главная кнопка «🕒 Вставить время урока» с живыми часами и две кнопки внизу: «🔄 Обновить меню» (`#refreshTocBtn`) и «🙈 Скрыть меню» / «👁️ Показать меню» (`#toggleTocBtn`). Большой макет со стримом логов отключен. Все папки плагина синхронизированы.
- [x] Выделенный урок на доске (`91:221` -> `91:866`, Daily Life & Routines A1) полностью перегенерирован и заменен на месте (`x: -5440, y: 9485`): квиз расширен до 10 вопросов с 4 вариантами ответов (A, B, C, D) и рандомизированным правильным ответом; упражнение Fill in the Blanks расширено до 10 предложений и 10 слов в банке слов; кнопка сброса изолирована строго внутри своего упражнения; старый блок удален.
- [x] Локализован сброс кнопки «🔄 СБРОС» строго до текущего упражнения (суб-блока): при клике внутри блока карточек в комплексном уроке сбрасываются только карточки этого упражнения без сброса и перемешивания остальных блоков урока.
- [x] Внедрена обязательная рандомизация вариантов ответов в квизе: правильный ответ равномерно распределяется по позициям A, B, C, D (устранен баг «все ответы 1 пункт»).
- [x] Для квиза внедрено строго 4 варианта ответов (A, B, C, D) в генерации AI, фолбэках и верстке карточек плагина (`Math.min(opts.length, 4)`).
- [x] Введено железное правило: в упражнениях с пропусками (Fill in the Blanks) генерируется не менее 10 предложений (`count >= 10`).
- [x] Исправлен сброс разбросанных карточек Speaking Cards и плашек Fill Blanks (поиск `blockId` по всему `figma.currentPage` с возвратом оторванных детей в блок `appendChild` и сортировкой по `cardIndex`).
- [x] Интегрирован поиск картинок на Pinterest (`site:pinterest.com` + `i.pinimg.com` 736x/originals) в качестве приоритетного источника для квизов.
- [x] Введено железное правило: квизы генерируются строго не менее чем на 10 вопросов (`count >= 10`).
- [x] Внедрено точное распознавание упражнений с учебников и рабочих листов (Worksheet OCR): прямое преобразование предложений и банка слов в интерактивный блок без придумывания лишнего.
- [x] Реализовано точечное редактирование выделенного блока на холсте (замена картинок `UPDATE_BLOCK_IMAGES` без дублирования и замена контента на тех же координатах через `replaceNodeId`).
- [x] Решены проблемы перетаскивания и верстки карточек на холсте Figma: карточки Speaking Cards и плашки слов переведены на монолитные FrameNodes с заблокированными дочерними слоями (child.locked = true), плашки банка слов перемещены на верхний z-index поверх предложений, добавлены кнопки «СБРОС» для сброса стопки и слов, исправлены шрифты и шапки блоков (hdrH 72px, без наплыва на инструкции и без дублей уровней), добавлена поддержка детских картинок (котики/динозавры/лего) и диалоговое согласование объемов заданий в чате.
- [x] Исправлены ошибки генерации и контекста в чат-боте Telegram / Web UI: устранен краш NameError re в orchestrator.py, внедрен экстрактор контекста диалога _extract_conversation_context, добавлен 45-минутный методический план урока для A0 (10 лет) по темам «Предлоги места, описание людей и числа от 1 до 100», расширен content_fallback.py базами данных A0.
- [x] Протестирован и улучшен диалоговый режим Telegram бота: внедрена экспертная база педагогических знаний (pedagogical_knowledge.py), многошаговый диалог (вопрос методики -> подбор формата -> выбор темы -> создание урока), устранены зацикливания меню и странные шаблонные ответы.
- [x] Реализован полноценный диалоговый чат в Telegram боте через chat_agent.py (обсуждение проектов, методики, быстрые inline-кнопки, запуск отрисовки по кнопке «🚀 Нарисовать на доске»).
- [x] Синхронизирована общая история диалога между Web UI панелью управления и Telegram ботом (GET/DELETE /api/chat/history, отображение отправителя «Вы (Telegram)»).
- [x] Устранено всплывание окна AntigravityCLI на Windows через системный флаг CREATE_NO_WINDOW и защиту от зависания неавторизованных процессов.
- [x] Внедрена Таксономия Блума (Bloom's Taxonomy) для комплексных уроков: модуль bloom_taxonomy.py, когнитивные бейджи на доске, педагогическая шпаргалка учителя, интеграция в Chat Agent, Web UI и Telegram бот.
- [x] Добавлена настройка SOCKS5/HTTP прокси сервера в панель управления Telegram-ботом для обхода блокировок api.telegram.org.
- [x] Устранена ошибка циклов событий «Cannot close a running event loop» в Telegram боте через управляемый асинхронный пайплайн Application.
- [x] Реализовано лаконичное inline-меню Telegram бота (выбор доски, выбор ученика, быстрые действия, генерация заданий).

---
*Правило тактической памяти: Выполненные пункты переносятся в «Недавно выполнено» и удаляются при следующем сеансе.*
